# راهنمای کدنویسی ربات

**How to write the robot's Python.** این سند مکمل [`RULES.md`](RULES.md) است:
آنجا قانون مسابقه است، اینجا زبانِ نوشتن مغز ربات. هرچه اینجا آمده با
`pyreader.js` — همان مفسری که مسابقه واقعاً با آن کد شما را اجرا می‌کند —
تطبیق داده شده است.

---

## ۱. برنامه چطور اجرا می‌شود

برنامه‌ی شما **۱۰ بار در ثانیه، از خط اول تا خط آخر** اجرا می‌شود. حلقه‌ی
اصلی لازم ندارید — بازی خودش حلقه است.

هر بار که برنامه اجرا می‌شود:

1. همه‌ی سنسورها با مقدار تازه پر می‌شوند.
2. کد شما از بالا تا پایین یک بار می‌دود.
3. هر عددی که در `wheelleft` و `wheelright` گذاشته باشید به موتورها می‌رود.

بین دو اجرا، چرخ‌ها **آخرین دستور را نگه می‌دارند**. یعنی اگر یک بار
`wheelleft = 25` بگذارید و دفعه‌ی بعد چیزی نگذارید، ربات همچنان جلو می‌رود.

> چون هر ثانیه ۱۰ بار اجرا می‌شود، `timer = 10` دقیقاً یعنی «یک ثانیه».

---

## ۲. کوچک‌ترین برنامه‌ی ممکن

```python
wheelleft = 25
wheelright = 25
```

این ربات مستقیم می‌رود تا به دیوار بخورد. برنامه‌ی واقعی یک پله بالاتر است:

```python
if front < 40:
    wheelleft = 25
    wheelright = -25
else:
    wheelleft = 25
    wheelright = 25
```

«اگر جلویم نزدیک‌تر از ۴۰ سانتی‌متر چیزی هست بچرخ، وگرنه برو جلو.»
**تمام برنامه‌های خوب همین شکل‌اند: یک نردبان `if / elif / else`.**

---

## ۳. چرخ‌ها

`wheelleft` و `wheelright` بین **`-25`** و **`25`**. بیرون این بازه بریده
می‌شود — `wheelleft = 9999` هم فقط ۲۵ است.

| چپ | راست | نتیجه |
|---|---|---|
| `25` | `25` | جلو، تمام سرعت |
| `12` | `12` | جلو، نصف سرعت |
| `-25` | `-25` | عقب |
| `25` | `-25` | چرخش به راست، درجا |
| `-25` | `25` | چرخش به چپ، درجا |
| `25` | `10` | قوس ملایم به راست، در حال حرکت |

**چقدر می‌چرخم؟**

    درجه  =  |wheelright − wheelleft| × 4.58 × ثانیه

مثلاً `18 / -18` به مدت یک ثانیه ≈ **۱۶۵ درجه**. این عدد **تقریبی** است: ربات
که به دیوار تکیه داده باشد کمی کمتر می‌چرخد.

---

## ۴. زمان: `movetime` و `seconds()`

یک اجرا فقط یک‌دهم ثانیه طول می‌کشد. برای «این کار را ۲ ثانیه ادامه بده»:

```python
if bumperfront == 1:
    wheelleft = -25
    wheelright = -25
    movetime = seconds(2)      # همین چرخ‌ها را ۲ ثانیه نگه دار
```

تا وقتی `movetime` تمام نشده، **چرخ‌ها قفل‌اند** و هر چیزی که بعداً بنویسید
نادیده گرفته می‌شود. `movetime` خودش خوانا هم هست: تعداد قدم‌های باقی‌مانده.

### حرکت‌های آماده

```python
forward(2)      # ۲ ثانیه جلو
backward(1.5)   # ۱٫۵ ثانیه عقب
turnleft(1)     # ۱ ثانیه چرخش به چپ  (18 / -18)
turnright(1)
stop(0.5)
```

تا وقتی یک حرکت آماده در حال اجراست، **فراخوانی حرکت جدید نادیده گرفته
می‌شود** — پس نگران این نباشید که هر یک‌دهم ثانیه دوباره صدایش می‌زنید.

---

## ۵. حافظه: `timer` و `state`

تمام متغیرهای سنسوری هر بار از نو نوشته می‌شوند. **فقط `timer` و `state`**
بین اجراها باقی می‌مانند و از صفر شروع می‌شوند. (هر اسم دلخواه دیگری هم که
خودتان بسازید باقی می‌ماند، ولی این دو رسماً برای همین‌اند.)

```python
timer += 1

if timer > 100:        # ۱۰ ثانیه گذشت
    state = 1
    timer = 0
```

### ماشین حالت — الگوی اصلی برنامه‌های جدی

```python
# state 0 = تمیز کردن   1 = برگشت به خانه   2 = شارژ گرفتن

if state == 0:
    if battery < 40:
        state = 1
    else:
        wheelleft = 25
        wheelright = 25

elif state == 1:
    goto(dockx, docky)
    if atgoal == 1:
        state = 2

elif state == 2:
    wheelleft = 0
    wheelright = 0
    if battery > 95:
        state = 0
```

چرا این‌قدر مهم است؟ چون ربات همیشه می‌داند **وسط چه کاری بوده**. بدون آن،
ربات هر یک‌دهم ثانیه تصمیمش عوض می‌شود و هیچ‌وقت هیچ کاری را تمام نمی‌کند.

---

## ۶. سنسورها

**هر سنسوری که روی ربات نبسته باشید، مقدارش `-1` است.** این خودِ قانون است:
`goto()` بدون سنسور موقعیت کار نمی‌کند، `heading` بدون قطب‌نما `-1` است.

### فاصله (سانتی‌متر، حداکثر ۲۰۰)

`front` `frontleft` `frontright` `left` `right` `backleft` `backright`

و به ترتیبِ نصب روی بدنه: `dist1` … `distN` (و `sensors` = تعدادشان).
اسم‌های آشنا فقط میان‌بُرند — وقتی کار می‌کنند که واقعاً سنسوری آن طرف
بسته باشید.

### ضربه‌گیر

`bumper` = ۱ اگر همین حالا به چیزی چسبیده‌اید.
`bumperfront` / `bumperback` = کدام نیمه‌ی حلقه فشرده شد.

**چرا دو نیمه؟** چون ربات بدون چشمِ عقب، وقتی عقب‌عقب توی دیوار گیر کند
تنها راهِ فهمیدنش همین است — و پاسخ همیشگی به ضربه، یعنی «عقب برو»، در آن
حالت فقط بیشتر فشارش می‌دهد. پاسخ درست:

```python
if bumperfront == 1:
    backward(1)        # جلو خوردم → عقب برو
elif bumperback == 1:
    forward(1)         # عقب خوردم → جلو برو
```

### رنگ کف

`color` رنگ زیر ربات را می‌دهد. برای مقایسه این نام‌ها آماده‌اند:

| نام | عدد | یعنی |
|---|---|---|
| `white` | ۰ | کف کثیف — **اینجا امتیاز هست** |
| `red` | ۱ | تمیزشده توسط تیم قرمز |
| `blue` | ۲ | تمیزشده توسط تیم آبی |
| `green` | ۳ | فرش سبز — نصف سرعت، بی‌امتیاز |
| `black` | ۴ | مانع |
| `purple` | ۵ | فرش بنفش |

```python
if color == green:
    turnright(0.6)     # فرش امتیاز ندارد، از رویش رد نشو
```

### جهت و موقعیت

`heading` جهت شما، ۰ تا ۳۵۹ درجه (نیاز به **قطب‌نما**).
`x` و `y` موقعیت شما بر حسب **سانتی‌متر** از گوشه‌ی پایین-چپ (نیاز به
**سنسور موقعیت**).

### وضعیت مسابقه

`mytiles` `rivaltiles` `timeleft` — و روی نقشه‌های اتاق‌دار:
`room` (۰ = هال) و `clean1` / `clean2` / `clean3` (چند درصد از آن اتاق
رنگ من است).

### مخصوص U19

`battery` (۰ تا ۱۰۰) · `dockx` `docky` (پد شارژ، سانتی‌متر)
`dust` `dustmax` `dustfull` · `dumpx` `dumpy` (ایستگاه تخلیه)

### چه سنسوری در کدام رده؟

| | FS | U14 | U19 |
|---|---|---|---|
| `front` `frontleft` `frontright` | ✅ ثابت | ✅ | قابل خرید |
| `left` `right` `backleft` `backright` | ❌ | ❌ | قابل خرید |
| `bumper` `bumperfront` `bumperback` | ✅ ثابت | ✅ | قابل خرید |
| `color` | ✅ ثابت | ✅ | قابل خرید |
| `heading` | ❌ | ✅ | قابل خرید |
| `x` `y` و `goto()` | ❌ | ❌ | قابل خرید |
| `battery` `dockx` `docky` | ❌ | ❌ | ✅ |

> در FS و U14 نوشتن `goto(...)` خطا نمی‌دهد، ولی **هیچ کاری نمی‌کند**، چون
> `x` و `y` برابر `-1` هستند. این را با اجرای واقعی هم تست کردیم: ربات
> «Goto patrol» روی FS در ۶۰ ثانیه فقط ۲ کاشی گرفت.

---

## ۷. رفتن به یک نقطه (فقط با سنسور موقعیت)

| | |
|---|---|
| `goto(x, y)` | روی خط مستقیم به آن نقطه برو؛ خودش هر قدم فرمان می‌دهد |
| `stopgoto()` | بی‌خیالش شو، فرمان را پس بگیر |
| `distto(x, y)` | فاصله تا آن نقطه، سانتی‌متر |
| `angleto(x, y)` | زاویه‌ی آن نقطه، ۰ تا ۳۵۹ درجه |
| `atgoal` | ۱ می‌شود وقتی رسیدید |

مختصات‌ها همیشه **سانتی‌متر** از گوشه‌ی پایین-چپ‌اند — دقیقاً همان واحدی که
`dockx` / `docky` و `dumpx` / `dumpy` با آن می‌آیند.

`goto()` **خط مستقیم** می‌رود. مسیر واقعیِ خانه هرگز مستقیم نیست — دور مبل،
پشت سگ، بیرون آمدن از گوشه. پس همیشه حاشیه بگذارید:

```python
if state == 0 and battery < distto(dockx, docky) / 25 + 25:
    state = 1          # ۲۵٪ حاشیه‌ی چاق، چون مسیر مستقیم نیست
```

---

## ۸. پایتونی که پشتیبانی می‌شود

`pyreader.js` یک مفسر **کوچک** است، نه پایتون کامل.

**هست:**

* متغیر و انتساب: `=` `+=` `-=` `*=` `/=`
* `if` / `elif` / `else` و `while`
* مقایسه: `<` `>` `<=` `>=` `==` `!=`
* منطق: `and` `or` `not`
* ریاضی: `+` `-` `*` `/` `%` `//` `**` و پرانتز
* توابع: `abs` `min` `max` `int` `float` `round` `seconds`
* حرکت‌ها: `forward` `backward` `turnleft` `turnright` `stop`
* موقعیت: `goto` `stopgoto` `distto` `angleto`
* `True` `False` `None` و `pass`
* کامنت با `#`

**نیست:** `def`، `for`، `import`، `print`، لیست، دیکشنری، رشته، `try`،
`class`، `return`، `break`، `continue`.

**قواعد:**

* تورفتگی داخل `if` **چهار فاصله**.
* خطای نحوی **با شماره‌ی خط، قبل از شروع مسابقه** نشان داده می‌شود.
* اسم متغیری که وجود ندارد خطای زمان اجرا می‌دهد — و مفسر **نزدیک‌ترین اسم
  درست را پیشنهاد می‌کند**.
* یک اجرا حداکثر ۵۰۰٬۰۰۰ عمل، و هر `while` حداکثر ۲۰٬۰۰۰ دور. بیشتر از آن
  «حلقه‌ی بی‌نهایت» حساب می‌شود و متوقف می‌شود.

### بلوک `EDITOR SETUP`

اگر فایل را در VS Code می‌نویسید، خطوط قرمزِ «این متغیر تعریف نشده» آزاردهنده
است. راه‌حلِ خودِ مسابقه این است:

```python
# === EDITOR SETUP ==============================================
front = 200; frontleft = 200; frontright = 200
color = 0; battery = 100
# === END EDITOR SETUP ==========================================
```

بازی **کل این بلوک را نادیده می‌گیرد**. فقط برای ساکت کردن ویرایشگر است.

---

## ۹. سه برنامه‌ی کامل

### الف) پایه — دیوار را دور بزن (هر سه رده)

```python
if bumperfront == 1:
    wheelleft = -25
    wheelright = -25
    movetime = seconds(0.8)
elif bumperback == 1:
    wheelleft = 25
    wheelright = 25
    movetime = seconds(0.8)
elif front < 40:
    if frontleft > frontright:
        wheelleft = -18      # سمت چپ بازتر است، برو چپ
        wheelright = 18
    else:
        wheelleft = 18
        wheelright = -18
    movetime = seconds(0.5)
else:
    wheelleft = 25
    wheelright = 25
```

**قانون طلایی:** همیشه **از سمتی که چیزی دیده، دور شو**.

### ب) U14 — دنبال کف کثیف بگرد

```python
if bumperfront == 1:
    backward(0.8)
elif front < 35:
    turnright(0.5)
elif color == green or color == purple:
    forward(0.6)              # فرش امتیاز ندارد، ردش کن
elif color == red or color == blue:
    if timer > 20:            # ۲ ثانیه است روی کف تمیز می‌گردم
        turnleft(0.4)
        timer = 0
    else:
        timer += 1
        wheelleft = 25
        wheelright = 25
else:
    timer = 0                 # کف سفید! همین‌جا بمان
    wheelleft = 25
    wheelright = 25
```

### ج) U19 — سفر برگشت به پد شارژ

```python
timer += 1

if state == 0:                                  # تمیز کردن
    if battery < distto(dockx, docky) / 25 + 25:
        state = 1
        stopgoto()
    elif bumperfront == 1:
        backward(0.8)
    elif front < 40:
        turnright(0.5)
    else:
        wheelleft = 25
        wheelright = 25

elif state == 1:                                # برگشت
    goto(dockx, docky)
    if atgoal == 1 or battery > 98:
        state = 2
    if bumperfront == 1:                        # مانع سر راه خانه
        stopgoto()
        backward(0.6)
        state = 1

else:                                           # روی پد
    wheelleft = 0
    wheelright = 0
    if battery > 95:
        state = 0
```

**یادتان باشد:** باتری صفر یعنی **مرگ قطعی** — ربات تا آخر مسابقه همان‌جا
می‌ماند. و ساعتِ «گیر کردن» موقع شارژ متوقف است، ولی به‌محض رسیدن به ۱۰۰٪
دوباره راه می‌افتد؛ **چادر زدن روی پد جواب نمی‌دهد**.

---

## ۱۰. اشتباه‌های رایج

| اشتباه | چه می‌شود | درست |
|---|---|---|
| `while True:` برای حلقه‌ی اصلی | «حلقه‌ی بی‌نهایت» و توقف کد | حلقه لازم نیست — بازی خودش هر ۰٫۱ ثانیه کدتان را صدا می‌زند |
| جواب دادن به `bumper` با «عقب برو» | اگر از عقب گیر کرده باشید، بیشتر فشارش می‌دهید | `bumperfront` و `bumperback` را جدا جواب بدهید |
| `if front < 40` وقتی سنسور جلو نبسته‌اید | `front` برابر `-1` است، پس شرط **همیشه درست** است | اول سنسور را بخرید، یا `front > 0 and front < 40` |
| `goto()` در FS یا U14 | هیچ اتفاقی نمی‌افتد | این رده‌ها سنسور موقعیت ندارند |
| تورفتگی با Tab یا ۲ فاصله | خطای «unexpected indentation» | دقیقاً **چهار فاصله** |
| استفاده از `state` برای شمارش | با منطق حالت قاطی می‌شود | `timer` را برای شمردن بگذارید |
| خروج زیادی دیر برای شارژ | ربات وسط راه می‌میرد | حاشیه‌ی چاق بگذارید و **زود** راه بیفتید |

---

## ۱۱. آزمودن بدون نوشتن حتی یک خط

روی صفحه‌ی آماده‌سازی، منوی **«sample code»** شش مغز آماده دارد:
**Wall-follow** · **Easy moves** · **Hunter** · **Colour sensor** ·
**Goto patrol** · **Battery runner**. هرکدام را بارگذاری کنید، بخوانید،
دستکاری کنید. فایل‌هایشان در `leagues/vacuum/bots/` است.

بچه‌های **FS** به‌جای ویرایشگر، دکمه‌ی **🤖 AI** را می‌زنند: یک تصویر از ربات،
و برای هر سنسور یک سؤال — «این چشم چیزی دید، کدام طرف بروم؟». خروجی همین
پایتونی است که در این سند توضیح داده شد.
