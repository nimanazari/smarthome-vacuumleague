# 🏁 نقشه‌های مسابقه · The competition maps

شش پوشه، شش نقشه: `map1/` تا `map6/` — هر کدام یک `map.json`.
این پوشه فقط در کیت برگزارکننده است (AdminKit) و **هرگز در TeamKit یا
ریپوی عمومی تیم‌ها نیست**؛ بچه‌ها نقشه‌ها را در روز مسابقه می‌بینند —
یا اگر بخواهی، از سایت (بخش پایین).

Six folders, six maps, one `map.json` each. Organiser kit only — never
in the TeamKit or the public teams repo.

## چطور کار می‌کند · how it works

1. `serve.bat` را بزن. چون `tools/mapserver.py` کنارش هست، همان بازی با یک
   قابلیت اضافه بالا می‌آید: خواندن و **ذخیره‌ی** این پنج پوشه.
2. در بازی، منوی نقشه گزینه‌های 🏁 «مپ ۱ تا ۶» دارد (فقط آن‌هایی که
   روشن‌اند). کنارش کلید **🚪 درها** است: «طبق نقشه»، «همه باز» (درِ
   هل‌دادنی حذف می‌شود)، «همه بسته».
3. در مپ‌ساز (دکمه‌ی ✏️ کنار نقشه، یا `leagues/vacuum/mapmaker.html`)
   بخش **«🏁 نقشه‌های مسابقه»** هست: هر مپ را باز کن، ویرایش کن، «ذخیره در
   مپ N» (یا Ctrl+S) — همان لحظه در `mapN/map.json` نوشته می‌شود و نسخه‌ی
   قبلی به `map.json.bak` می‌رود. دکمه‌ی ● روشن / ○ خاموش هر مپ را از لیست
   بازی بیرون می‌برد یا برمی‌گرداند (یک فایل خالی به اسم `OFF` در پوشه).
4. تیک «🚪 درها کامل باز باشن» در مپ‌ساز، در خودِ نقشه ثبت می‌شود
   (`doorsOpen: true`) — مپ ۱ این‌طور ساخته شده.
5. بعد از هر ویرایش: `node tools/validate-map.js map1` (یا map2 … map6).

## ریلیز مپ به تیم‌ها · handing a map to the teams

**ساده‌ترین راه (نصب = کپی یک فایل):** پوشه‌ی `handout/` برای هر مپ
`mapN.json` و `mapN.html` دارد. فایل `map1.json` را به تیم بده؛ تیم آن را
**کنار `serve.bat`** (یا کنار `.exe`) کپی می‌کند و بازی را باز می‌کند — بازی
خودش پیدا می‌کند، با 📁 در منوی نقشه می‌آید و انتخاب می‌شود. هیچ منویی لازم
نیست. `map1..map5` هر پنج تا را می‌شناسد؛ پوشه‌ی `maps/` کنار بازی هم قبول است.
*Copy `mapN.json` next to the game; it is found and selected on start.*


کنار هر `map.json` یک **`map.html`** هم هست (با `python tools/make-map-html.py`
ساخته می‌شود؛ release.py خودش می‌سازد). این یک فایل مستقل است: نقشه‌ی
کشیده‌شده با اسم وسایل و شماره‌ی اتاق‌ها، مشخصات، دکمه‌ی چاپ، **دانلود JSON**
و **▶ اجرا در بازی**. همین یک فایل را به تیم‌ها بده (تلگرام، سایت، فلش):

- تیم فایل را باز می‌کند و نقشه را می‌بیند (هیچ چیز دیگری لازم نیست).
- در بازی: منوی ⚙️ نقشه و تنظیمات → **«لود فایل مپ»** → همین `map.html` را
  انتخاب می‌کند (JSON هم قبول می‌کند) — نقشه‌ی رده همان می‌شود.
- اگر فایل را کنار `index.html` کیت بگذارد و با `serve.bat` باز کند، دکمه‌ی
  «اجرا در بازی» مستقیم بازی را با همان نقشه بالا می‌آورد.

`index.html` این پوشه هم فهرست هر پنج مپ است.

*Each map also ships as a self-contained `map.html` (plan + facts + JSON
inside). Hand that one file to the teams; the game's «لود فایل مپ» opens it.*

## روی سایت · on the site

همین شش اسلات روی smarthomeleague.ir هم هست: `/admin` → تب **Maps** →
روشن/خاموش، **edit** (مپ‌ساز روی سایت، فقط ادمین)، **upload map.json**
(همین فایل‌ها) و **download**. بازیِ `/game/` سایت همان 🏁ها را نشان
می‌دهد. نسخه‌ی اولیه‌ی هر شش مپ با `competition-maps.seed.json` همراه
سایت می‌رود؛ هر ذخیره‌ی ادمین در `data/competition-maps.json` سرور
می‌ماند و با دیپلوی بعدی پاک نمی‌شود.

The same six slots live on the site: `/admin` → **Maps** tab (on/off,
edit in the admin-only Map Maker, upload / download `map.json`).

## نقشه‌های فعلی · the current maps

| اسلات | نقشه | پلان | درها |
|---|---|---|---|
| مپ ۱ | خانه‌ی راهرودار · Corridor | پذیرایی راهرویی وسط، سه خواب در شمال، آشپزخانه/سرویس در جنوب | **همه باز** |
| مپ ۲ | خانه‌ی ویلایی · Villa | پذیرایی L شکل + راهروی شرقی، سه خواب روی هم کنار دیوار شرقی، محوطه‌ی سنگی | یک در (room 2) |
| مپ ۳ | خانه‌ی حیاط‌مرکزی · Courtyard | پذیرایی مربعی وسط، اتاق‌ها دورش، فویه‌ی جنوب‌شرقی | یک در (room 2) |
| مپ ۴ | خانه‌ی باغی · Garden | پذیرایی بزرگ شمال‌شرق، دو خواب روی هم در غرب، سرویس/فویه/آشپزخانه/خواب در جنوب — **ورودی‌ها ۴ کاشی (۲٫۵ متر)**، در ۳ کاشی | یک در گشاد (room 2) |
| مپ ۵ | خانه‌ی پهن · Wide | نوار پذیرایی وسط با تلویزیون آزاد، سرویس/خواب/آشپزخانه در شمال، دو خواب در گوشه‌های جنوب — **ورودی‌ها ۴–۵ کاشی (۲٫۵–۳٫۱ متر)**، در ۳ کاشی | یک در گشاد (room 2) |
| مپ ۶ | خانه‌ی باز ۴خوابه · Open 4-bedroom | **۲۶×۲۶ (بزرگ‌ترین)**، هال صلیبی، **۴ اتاق‌خواب** در چهار گوشه، آشپزخانه و سرویس در دو نوار میانی؛ همه‌ی ورودی‌ها ۴ کاشی | **همه باز، بدون فرش بنفش** |

مپ ۱ تا ۵ به استاندارد `leagues/vacuum/maps/MAP-STANDARD.md` ساخته شده‌اند و
اتاقِ دردار در همه‌شان `room == 2` است.

**مپ ۶ عمداً از استاندارد فاصله می‌گیرد** (خواستهٔ برگزارکننده): ۴ اتاق‌خواب
به‌جای ۳، هیچ در و هیچ فرش نشانه‌ی بنفشی ندارد، ورودی‌ها ۴ کاشی و زمین
۲۶×۲۶ است تا امتیازها بالا برود (۶۲۶ کاشی آزاد؛ در تست، مجموع امتیاز دو
تیم ۲۴۴ تا ۲۷۵ در برابر ۱۹۰ روی مپ ۳). شماره‌ی اتاق‌ها: ۱ آشپزخانه ·
۲/۳/۴/۵ خواب‌ها · ۶ سرویس (`clean6` هم به برنامه داده می‌شود).
