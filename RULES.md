# قوانین لیگ جاروبرقی هوشمند

**Smart Home Vacuum League — the rulebook.** هر عددی که در این سند آمده مستقیم از
کد همین پوشه خوانده شده و با اجرای واقعیِ مسابقه تأیید شده است. اگر قانونی را
عوض کردید، جدولِ «کدام قانون کجا زندگی می‌کند» در انتهای سند می‌گوید کدام فایل را
باید ویرایش کنید — و همین سند هم باید به‌روز شود.

---

## ۱. بازی در یک نگاه

دو ربات در یک خانه. هر کاشیِ کفی که ربات از رویش رد شود **رنگ آن ربات را
می‌گیرد و یک امتیاز دارد**. آخرین لمس صاحب کاشی است، پس رد شدن از روی کاشی
حریف یعنی **پس گرفتنش**. مسابقه با ساعت تمام می‌شود و هر طرف که کاشی بیشتری
داشته باشد برنده است.

فقط یک چیز کاشی را پس می‌گیرد: **جابه‌جایی توسط داور**.

---

## ۲. زمین بازی

هر سه رده روی **یک خانه‌ی بزرگ ۲۲×۲۲ اتاق‌دار** بازی می‌کنند
(`vacuum-rooms-22`) — یک آپارتمان واقعی با **شش ناحیه** و درهای ۲کاشی
(EN: a real family flat, six zones, 2-tile doorways):

| `room` | ناحیه · zone | کجا · where |
|---|---|---|
| ۰ | هال و پذیرایی · the hall / living | وسط و راستِ خانه |
| ۱ | آشپزخانه · kitchen | بالا-راست |
| ۲ | اتاق‌خواب ۱ · bedroom 1 | بالا-چپ |
| ۳ | اتاق‌خواب ۲ · bedroom 2 | پایین-چپ |
| ۴ | اتاق‌خواب ۳ · bedroom 3 | وسط-چپ |
| ۵ | سرویس بهداشتی · bathroom | پایین-راست |

متغیر `room` می‌گوید الان در کدام ناحیه‌ای و `clean1..clean5` درصد
تمیزیِ همان اتاق برای خودِ ربات است — در **هر سه رده**. دم هر در یک
فرش نشانه است تا سنسور رنگ هم «در» را بشناسد.

| | FS | U14 | U19 |
|---|---|---|---|
| نقشه | خانه‌ی اتاق‌دار ۲۲×۲۲ | خانه‌ی اتاق‌دار ۲۲×۲۲ | خانه‌ی اتاق‌دار ۲۲×۲۲ |
| ابعاد واقعی | ۱۳٫۷۵ × ۱۳٫۷۵ متر | ۱۳٫۷۵ × ۱۳٫۷۵ متر | ۱۳٫۷۵ × ۱۳٫۷۵ متر |
| کاشی‌های قابل تمیز شدن | **۴۳۱** | **۴۲۷** | **۴۲۷** |

* اندازه‌ی هر کاشی **۰٫۶۲۵ متر** است (`tileSize`).
* خانه‌ی ۲۲×۲۲ عمداً بزرگ‌تر از آن است که در ۳ دقیقه تمام شود — **مدیریت مسیر
  بخشی از مسابقه است**.
* **مبل، تخت، میز، قفسه، گلدان، پاف، سطل، کاسه‌ی حیوان، آباژور** موانع سفت‌اند.
  کاشیِ زیرشان اصلاً قابل تمیز شدن نیست و در مجموع بالا شمرده نشده.
* **فرش سبز / بنفش**: ربات با **نصف سرعت** رویش می‌رود، هیچ امتیازی ندارد و
  تمیز هم نمی‌شود. فقط یک مانع نرم است.
* **کف خیس**: دیگر در هیچ رده‌ای وجود ندارد — لکه‌های آب از خودِ نقشه‌ی هر
  سه رده برداشته شده‌اند.
* نقطه‌ی شروع: **تیم ۱ گوشه‌ی پایین-چپ**، **تیم ۲ گوشه‌ی بالا-راست**. شیر یا خط
  روی صفحه‌ی آماده‌سازی تعیین می‌کند چه کسی کدام گوشه را می‌گیرد.

> نقشه‌ی دلخواه: با دکمه‌ی 🛠 روی صفحه‌ی آماده‌سازی، **نقشه‌ساز همین لیگ**
> (`leagues/vacuum/mapmaker.html`) باز می‌شود. نقشه‌ی ساخته‌شده در همان مرورگر
> ذخیره می‌شود و بلافاصله قابل بازی است.
>
> **ویرایش نقشه‌ی فعلی:** دکمه‌ی ✏️ کنار انتخاب نقشه، **همان نقشه‌ای را که
> داری می‌بینی** — خانه‌ی رسمی هم — در نقشه‌ساز باز می‌کند: دیوار، مبلمان،
> کاشی/فرش، و خود ابعاد زمین (cols/rows/tileSize) همه قابل ویرایش‌اند و
> مستطیل اتاق‌ها هم روی بوم دیده می‌شوند. هر نقشه یک فایل جداست:
> `leagues/vacuum/maps/`.

---

## ۳. ربات

| مشخصه | مقدار |
|---|---|
| شعاع بدنه | ۰٫۲۴ متر |
| فاصله‌ی چرخ‌ها | ۰٫۵ متر |
| سرعت کامل | ۱ متر بر ثانیه (معادل عدد چرخ ۲۵) |
| برد سنسور فاصله | ۲ متر (۲۰۰ سانتی‌متر) |
| نرخ اجرای کد | **۱۰ بار در ثانیه** — پس `timer = 10` دقیقاً یک ثانیه است |
| بازه‌ی چرخ‌ها | `-25` تا `25`؛ بیرونِ این بازه بریده می‌شود |

فرمول تقریبی چرخش، همان که کل بازی با آن کار می‌کند:

    درجه چرخش  =  |wheelright − wheelleft| × 4.58 × ثانیه

**ساخت ربات (Robot Builder):** بودجه‌ی **۲۰۰ اعتبار**. هر قطعه‌ای که نبندی،
متغیرش در کد **`-1`** خوانده می‌شود — این خودِ قانون است، نه باگ.

| | FS | U14 | U19 |
|---|---|---|---|
| اجازه‌ی ساخت ربات | **ندارد** | دارد | دارد |
| ریگ سنسور | **ثابت** (`fs/kit.js`) | ریگ FS + قطب‌نما | آزاد (کیت کلاسیک پیش‌فرض) |

* **ریگ ثابت FS**: سه چشمِ جلو در زوایای `+۳۰°`، `۰°`، `−۳۰°`
  (`frontleft` / `front` / `frontright`)، **حلقه‌ی ضربه‌گیر** که دو نیمه‌اش جدا
  خوانده می‌شود (`bumperfront` / `bumperback`) و **سنسور رنگ** (`color`).
  تنها انتخاب یک تیم FS، **رنگ تیم** است.
* **U14**: همان ریگ به‌علاوه‌ی **قطب‌نما** (`heading`، ۰ تا ۳۵۹). سنسور موقعیت
  (GPS) و `goto()` عمداً بسته است.
* **U19**: فروشگاه قطعات باز است — سنسور موقعیت، دوربین، ژیروسکوپ، انکودر و
  بقیه در دسترس‌اند.

---

## ۴. امتیازدهی

1. هر کاشیِ کفِ تمیزشده = **۱ امتیاز**.
2. **آخرین لمس برنده است**: رد شدن از روی کاشیِ حریف آن را از او می‌گیرد
   (`steal` در هر سه زیرلیگ **روشن** است و هیچ زیرلیگی خاموشش نمی‌کند).
3. فرش و زیر مبلمان اصلاً شمرده نمی‌شوند.
4. تابلوی امتیاز درصد خانه را هم نشان می‌دهد: «چند درصد از خانه تمیز شده».

---

## ۵. جریمه‌ها و مداخله‌ی داور

| رویداد | شرط | هزینه |
|---|---|---|
| **گیر کردن (خودکار)** | ربات **۱۵ ثانیه** داخل دایره‌ای به شعاع **۰٫۸۵ متر** بماند | به نقطه‌ای تصادفی منتقل می‌شود و `penalty` کاشی از دست می‌دهد |
| **جابه‌جایی دستی داور** | دکمه‌ی Relocate در منوی ☰ | همان `penalty` کاشی |

`penalty` مقدارش در FS برابر **۲ کاشی** و در U14/U19 برابر **۵ کاشی** است.

* جریمه همیشه از **کفِ رنگ‌شده** برداشته می‌شود: به تعداد لازم کاشی سفید می‌شود
  و امتیاز پایین می‌آید. اگر تیم آن‌قدر کاشی نداشته باشد، فقط همان‌قدر که دارد
  از دست می‌دهد.
* **در حال شارژ، ساعتِ گیر کردن متوقف است** — ولی به‌محض رسیدن به ۱۰۰٪ دوباره
  راه می‌افتد. **چادر زدن روی پد، تمیز کردن نیست.**
* **باتری خالی، گیر کردن نیست**: ربات مرده جابه‌جا نمی‌شود و جریمه نمی‌خورد.
* بقیه‌ی کنترل‌های داور در منوی ☰: توقف، پایان فوری مسابقه، سرعت ۲ برابر.

---

## ۶. پایان مسابقه و تساوی

* مسابقه با تمام شدن ساعت تمام می‌شود؛ صاحب کاشی بیشتر برنده است.
* **تساوی مسابقه را تمام نمی‌کند.** وقت اضافه به ترتیب اضافه می‌شود:

      +10 ثانیه ، سپس +5 ، +5 ، +5 ، +5 ، +5

  یعنی حداکثر **۶ نوبت وقت اضافه، جمعاً ۳۵ ثانیه**. اگر بعد از همه‌ی این‌ها باز
  هم مساوی بود، نتیجه **تساوی** ثبت می‌شود.
* «پایان فوری» داور وقت اضافه نمی‌دهد — نتیجه همان لحظه قطعی است.

---

## ۷. سه زیرلیگ

| | **FS** | **U14** | **U19** |
|---|---|---|---|
| رده‌ی سنی | ۸ تا ۱۱ سال | ۱۱ تا ۱۴ سال | ۱۴ تا ۱۹ سال |
| زمان مسابقه | **۱۲۰ ثانیه** | **۱۸۰ ثانیه** | **۱۸۰ ثانیه** |
| گربه و سگ (`pets`) | خاموش | روشن | روشن |
| کف خیس (`wet`) | خاموش — لکه‌ها اصلاً روی نقشه نیستند | خاموش — همین‌طور | خاموش — همین‌طور |
| دزدیدن کاشی (`steal`) | روشن | روشن | روشن |
| هزینه‌ی جابه‌جایی (`penalty`) | ۲ کاشی | ۵ کاشی | ۵ کاشی |
| باتری و پد شارژ (`battery`) | خاموش | خاموش | **روشن** |
| مخزن خاک (`dump`) | خاموش | خاموش | **روشن** |
| ساخت ربات | ندارد | دارد | دارد |
| نوشتن برنامه | **صفحه‌ی 🤖 AI** | ویرایشگر یا 🤖 AI | ویرایشگر |
| برنامه‌ی نمونه | `fs/program.py` | `u14/program.py` | `u19/program.py` |

### ۷٫۱ — FS · فرست استپ (۸ تا ۱۱ سال)

خانه‌ی آرام. **نه گربه، نه سگ** — و مانندِ هر سه رده، لکه‌ی آبی هم روی
زمین نیست: کف خیسی که تمیز نمی‌شود ولی جریمه هم ندارد فقط بچه را گیج
می‌کند. جریمه‌ی جابه‌جایی ملایم است (۲ کاشی) و مسابقه ۲ دقیقه‌ای است.

بچه ربات نمی‌سازد؛ همه با **یک ربات یکسان** بازی می‌کنند و تنها انتخاب،
**رنگ تیم** است — همان رنگی که هم بدنه و هم کاشی‌های تمیزشده را رنگ می‌کند.

**صفحه‌ی 🤖 AI** (`leagues/vacuum/helper/helper.html`) جای ویرایشگر را می‌گیرد
(EN: the AI helper page replaces the editor for FS; U14 opens the same page
with `?league=u14` and gains the compass, the room number, the clean-% and
the absolute **Turn-to-°** with 3-move exit plans):
یک تصویر از ربات، و برای هر سنسور یک سؤال — «این چشم چیزی دید، کدام طرف بروم؟».
خروجی، یک فایل پایتونِ واقعی است که همان مسابقه اجرایش می‌کند. صفحه هیچ
الگوریتم آماده‌ای نمی‌دهد: بچه خودش قانون‌ها را می‌سازد و **ترتیبشان همان ترتیب
زنجیره‌ی `if/elif`** است.

### ۷٫۲ — U14 (۱۱ تا ۱۴ سال)

خانه‌ی کامل، و خانه‌ی بزرگ (۲۲×۲۲، ۴۳۹ کاشی).

* **گربه و سگ** موانع متحرک‌اند: گربه شعاع ۰٫۱۹ متر با سرعت ۰٫۵ متر بر ثانیه،
  سگ شعاع ۰٫۲۴ متر با سرعت ۰٫۶۲ متر بر ثانیه. راه می‌روند، مکث می‌کنند، و اگر
  گیر کنند خودشان راه فرار پیدا می‌کنند.
* **قطب‌نما** اضافه می‌شود: `heading` بین ۰ تا ۳۵۹.
* **حالت فینال** در دسترس است (بخش ۸).

### ۷٫۳ — U19 (۱۴ تا ۱۹ سال)

هرچه در U14 هست، به‌علاوه‌ی **باتری واقعی** و **مخزن خاک**.

**باتری**

| | |
|---|---|
| مصرف با تمام گاز | `100/60` درصد بر ثانیه ⟵ **شارژ کامل ≈ ۶۰ ثانیه رانندگی** |
| مصرف در حال سکون | یک‌چهارم مصرف رانندگی (≈ ۰٫۴۲٪ بر ثانیه) |
| شارژ روی پد | **۲۵٪ بر ثانیه** ⟵ صفر تا صد در **۴ ثانیه** |
| شعاع پد | ۰٫۳۲ متر |
| باتری صفر | **`limp: 0` — ربات همان‌جا برای همیشه می‌ایستد** |

* پد شارژ **جای ثابت** دارد و مختصاتش (`dockx` / `docky`، بر حسب سانتی‌متر) به
  **هر دو ربات** فرستاده می‌شود. روی خانه‌ی بزرگ، پد یک شیء نقشه است در
  (۹٫۹۰ ، ۸٫۱۷) با دهانه‌ی رو به غرب.
* مرگ باتری **قطعی** است. کل مهارت این رده، **برنامه‌ریزی سفر برگشت** است.
  تخمین دم‌دستی: `distto(dockx, docky) / 25` درصد شارژ لازم است — به‌علاوه‌ی یک
  حاشیه‌ی چاق، چون `goto()` خط مستقیم می‌رود و مسیر واقعی هرگز مستقیم نیست.

**مخزن خاک**

* **مسابقه‌ی دو نفره:** مخزن هرگز پر نمی‌شود. رسیدن به **ایستگاه تخلیه** فقط
  **یک بار** ارزش دارد: **+۵ امتیاز** روی مجموع نهایی.
* **مسابقه‌ی تک‌نفره:** مخزن **۳۰ کاشی** جا دارد. به‌محض پر شدن، ربات **هیچ
  کاشی‌ای تمیز نمی‌کند** — می‌راند، ولی کف همان‌قدر کثیف می‌ماند و امتیاز
  متوقف می‌شود — تا وقتی به ایستگاه تخلیه برسد و خالی کند.
* ایستگاه تخلیه **یک دستگاه جداست و هیچ ربطی به پد شارژ ندارد**؛ روی خانه‌ی
  بزرگ در گوشه‌ی شمال-غربی، (۰٫۹۰ ، ۱۲٫۶۰). **دو مأموریت، دو سفر، سه دقیقه.**
* متغیرهای کد: `dust`، `dustmax`، `dustfull`، `dumpx`، `dumpy`.
* اگر نقشه‌ای اصلاً ایستگاه تخلیه نداشته باشد، این قانون خاموش می‌ماند — ربات
  روی ۳۰ کاشی حبس نمی‌شود.

---

## ۸. تنظیمات داور (روی صفحه‌ی آماده‌سازی)

| تنظیم | پیش‌فرض | معنی |
|---|---|---|
| ⏱ زمان | ۱۲۰ ثانیه در FS، ۱۸۰ در U14/U19 | طول مسابقه |
| 👥 تعداد بازیکن | ۲ | با ۱، فقط ربات قرمز بازی می‌کند (و در U19 قانون مخزن فعال می‌شود) |
| 🐾 Pets | **خاموش** | گربه و سگ را وارد خانه می‌کند. **این تیک، قانونِ زیرلیگ را بازنویسی می‌کند** — پس یک مسابقه‌ی U14 بدون تیک زدن، بدون حیوان خانگی برگزار می‌شود |
| ✅ FINAL mode | خاموش | **گربه پاک‌کن می‌شود**: هر کاشیِ رنگ‌شده‌ای که از رویش رد شود دوباره سفید می‌شود و صاحبش آن امتیاز را از دست می‌دهد. برای فینال‌ها. در FS بی‌اثر است (گربه‌ای وجود ندارد) |
| ⚡ Dock (فقط U19) | خاموش | دور پد شارژ **سه دیوار** می‌کشد که فقط از یک طرف باز است — حریف نمی‌تواند رباتِ در حال شارژ را از روی پد هل بدهد |
| 🗺 نقشه | خانه‌ی رسمی هر رده | نقشه‌های خانگی (مثل «خانه‌ی اتاق‌دار») و نقشه‌های ساخته‌شده در نقشه‌ساز هم اینجا فهرست می‌شوند |

---

## ۹. آنچه کد ربات می‌بیند

> این بخش فقط **فهرست مرجع** است. راهنمای کامل کدنویسی — با مثال‌های اجراشده،
> ماشین حالت، و اشتباه‌های رایج — در [`CODING.md`](CODING.md) است.

هر متغیری که سنسورش روی ربات بسته نشده باشد، **`-1`** است.

**فاصله (سانتی‌متر، حداکثر ۲۰۰):** `front` `frontleft` `frontright` `left`
`right` `backleft` `backright` — و به ترتیب نصب: `dist1` … `distN`

**وضعیت:** `bumper` `bumperfront` `bumperback` · `color` · `heading` · `x` `y`
· `room` · `clean1` `clean2` `clean3`

**رنگ‌ها برای مقایسه:** `white=0` `red=1` `blue=2` `green=3` `black=4` `purple=5`

**مسابقه:** `mytiles` `rivaltiles` `timeleft`

**U19:** `battery` · `dockx` `docky` · `dust` `dustmax` `dustfull` · `dumpx` `dumpy`

**حافظه‌ی بین قدم‌ها (فقط این دو، از صفر شروع می‌شوند):** `timer` `state`

**خروجی:** `wheelleft` و `wheelright`، هر کدام بین `-25` و `25`.

**حرکت‌های آماده:** `forward` `backward` `turnleft` `turnright` `stop`، و
`movetime = seconds(n)` برای نگه داشتن همان چرخ‌ها به مدت n ثانیه.
**خانواده‌ی GPS** (`goto` `distto` `angleto` `stopgoto`) فقط با سنسور موقعیت
کار می‌کند — یعنی عملاً فقط در U19.

**پایتونِ پشتیبانی‌شده:** `if / elif / else`، `while`، مقایسه‌ها،
`and / or / not`، ریاضیات، و توابع `abs, min, max, int, float, round, seconds`.
تورفتگی داخل `if` چهار فاصله است. خطای نحوی، **با شماره‌ی خط، قبل از شروع
مسابقه** اعلام می‌شود.

---

## ۱۰. اجرا

باید روی **HTTP** سرو شود. باز کردن مستقیم `index.html` از روی دیسک کار
نمی‌کند، چون مرورگر اجازه‌ی خواندن فایل‌های `.py` را نمی‌دهد.

```bash
python -m http.server 8801
```

بعد `http://localhost:8801/` را باز کنید. (یا فقط روی `serve.bat` دوبار
کلیک کنید.)

---

## ۱۱. کدام قانون کجا زندگی می‌کند

| چه چیزی را می‌خواهید عوض کنید | تنها فایلی که باید ویرایش شود |
|---|---|
| قانون یا زمان یک رده‌ی سنی | `rules:` / `defaults:` در `leagues/vacuum/<div>/rules.js` |
| موتور امتیازدهی، جریمه‌ها، کار گربه در حالت فینال | `VacuumMode` در `leagues/vacuum/league.js` |
| اعداد باتری و جای پد | `BATTERY` در `leagues/vacuum/league.js` |
| ظرفیت مخزن | `DUMP_EVERY` در `league.js` و `dumpEvery` در `u19/rules.js` |
| وقت اضافه و جریمه‌های پایه | `OVERTIME_STEPS` / `PENALTY_TILES` / `WET_PENALTY` در `league.js` |
| خانه: دیوار، مبلمان، فرش، نقطه‌ی شروع | `HOUSE` / `GROWN` / `ROOMS` در `league.js` |
| ساعت گیر کردن و شعاعش | `STUCK_SECONDS` / `STUCK_RADIUS` در `physics.js` |
| سرعت فرش | `RUG_SPEED` در `physics.js` |
| سنسورهای ثابت FS | `leagues/vacuum/fs/kit.js` (هم صفحه‌ی AI و هم موتور از همین می‌خوانند) |
| برنامه‌ی نمونه‌ی یک رده | `leagues/vacuum/<div>/program.py` |
| ربات‌های آماده‌ی منوی انتخاب | `bots:` در `league.js` + فایلش در `bots/` |

**هرگز** برای یک ترجیحِ مخصوصِ این لیگ، فایل‌های مشترک (`physics.js`،
`engine.js`، `index.html`) را ویرایش نکنید مگر آنکه واقعاً قانونِ همه‌ی
لیگ‌ها باشد — هر سطر جدول بالا کانالِ رسمیِ همان تغییر است.

---

## ۱۲. برگزاری رسمی (از ۲۱ مرداد ۱۴۰۵)

قواعد رویدادهای رسمی — پشتوانه‌ی فنی هر بند در خود بازی ساخته شده است:

1. **نتیجه‌ی رسمی فقط از رایانه‌ی داور.** فیزیک بازی با گام ثابت ۱/۶۰ ثانیه
   اجرا می‌شود و به فریم‌ریت مانیتور وابسته نیست؛ با این حال اختلاف
   ممیزشناور بین مرورگرها/سخت‌افزارها ممکن است — مرجعِ نتیجه، ایستگاه داور است.
2. **بذر مسابقه (Seed).** داور می‌تواند در «نقشه و تنظیمات» بذر عددی بدهد؛
   خالی بماند، بذرِ تصادفی ساخته می‌شود. در هر دو حالت بذرِ استفاده‌شده در
   دفتر مسابقات و گزارش ثبت می‌شود. «بذر + نقشه + دو فایل کد» یک مسابقه را
   عیناً بازتولید می‌کند (مقصد و جهتِ جابه‌جایی‌های داور هم از همین بذر است).
3. **بودجه‌ی پردازش.** میانگین و بیشینه‌ی زمان CPU هر برنامه در گزارش مسابقه
   ثبت می‌شود. برنامه‌ای که مکرراً بیش از ۱۵ میلی‌ثانیه در هر تیک بسوزاند
   (ستون slow ticks) با تذکر داور روبه‌رو می‌شود؛ تکرار = باخت فنی آن بازی.
4. **خطای برنامه.** خطای زمان اجرا چرخ‌های همان تیک را صفر می‌کند و در
   گزارش شمرده می‌شود (crash ticks). برنامه‌ای که بیش از ۲۰٪ تیک‌های مسابقه
   خطا بدهد، به تشخیص داور بازنده‌ی فنی است.
5. **حالت مسابقه‌ی رسمی (🔒).** پیش از هر بازی رسمی داور از منو
   «Official mode» را با PIN فعال می‌کند: ویرایش زنده‌ی خانه، سرعت ۲x،
   جابه‌جایی با Shift+drag و هر راه تعویض کد (بارگذاری فایل، منوی ربات‌ها،
   صفحه‌ی AI) قفل می‌شود — این همان «انجماد کد» است.
6. **هویت کد.** هنگام بارگذاری هر فایل، ۱۲ رقم اول SHA-256 آن کنار نامش
   نمایش داده می‌شود و هشِ کدی که واقعاً مسابقه داد در رکورد ثبت می‌شود؛
   رسیدگی به اعتراض = مقایسه‌ی همین دو هش.
7. **جابه‌جایی داور.** دکمه‌ی جابه‌جایی منو دو حالت دارد: «با جریمه» (قانون
   بند ۵ همین کتابچه) و «بدون جریمه» برای گره‌گشایی‌هایی که تقصیر تیم نیست.
   هر دو با زمانِ مسابقه در گزارش ثبت می‌شوند. Shift+drag ابزار تمرین است و
   در حالت رسمی قفل است.
8. **شروع اشتباه.** «Restart match» شمارش معکوس یا بازیِ در جریان را باطل
   می‌کند و همان تنظیمات را از نو شروع می‌کند؛ اجرای باطل‌شده هیچ‌جا ثبت
   نمی‌شود.
9. **توقف.** دکمه‌ی Stop ساعت را نگه می‌دارد، بنر «⏸ توقف بازی» برای سالن
   نمایش داده می‌شود و لحظه‌ی توقف/ادامه در گزارش ثبت می‌شود.
10. **بازی حذفی مساوی نمی‌شود.** بازی‌های جام پس از نردبان وقت اضافه، در
    حالت «مرگ ناگهانی» هر بار +۵ ثانیه ادامه می‌یابند تا یک تیم جلو بیفتد؛
    فقط End Game داور می‌تواند آن را قطع کند.
11. **ثبت و خروجی.** هر مسابقه‌ی واقعی خودکار در «دفتر مسابقات» ثبت می‌شود
    (خروجی CSV) و گزارش هر مسابقه با PNG نقشه‌ی حرارتی + JSON کاملِ وقایع
    قابل دانلود و پیوستِ پرونده‌ی اعتراض است.

---

## ۱۳. تناقض‌های شناخته‌شده‌ی مستندات

این‌ها **در کد درست‌اند** ولی در متن‌های قدیمی‌تر اشتباه نوشته شده‌اند:

1. **تعداد کاشی‌ها.** `leagues/vacuum/README.md` می‌گوید U14/U19 روی ۲۲۷ کاشی
   بازی می‌کنند. آن عدد مال زمانی است که هر سه رده روی خانه‌ی ۱۶×۱۶ بازی
   می‌کردند. اکنون U14/U19 روی خانه‌ی ۲۲×۲۲ و **۴۳۹ کاشی** بازی می‌کنند
   (FS همان **۲۳۱** است). *اندازه‌گیری‌شده با اجرای واقعی.*
2. **ساعت گیر کردن.** جدول راهنمای داخل بازی (`index.html`) می‌گوید «۱۵ ثانیه»
   و «۵ کاشی». کد `STUCK_SECONDS = 10` است و جریمه در FS **۲ کاشی**.
3. **جای پد شارژ.** `leagues/vacuum/README.md` مختصات ثابت
   (۷٫۱۸۷۵ ، ۵٫۹۳۷۵) را می‌دهد؛ آن فقط **پشتیبانِ** حالتی است که نقشه شیء
   `dock` نداشته باشد. روی خانه‌ی بزرگ که U14/U19 با آن بازی می‌کنند، پد شیء
   نقشه است و در (۹٫۹۰ ، ۸٫۱۷) می‌ایستد.
4. **`MODULARITY.md`** که در `README.md` و در سرصفحه‌ی `league.js` به آن ارجاع
   داده شده، در این نسخه‌ی مستقل وجود ندارد.
