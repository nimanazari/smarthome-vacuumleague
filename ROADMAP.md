# نقشه‌ی راه — از بازی آموزشی تا لیگ مسابقاتی واقعی

*تاریخ: ۲۱ مرداد ۱۴۰۵ — 2026-08-12.*

> **به‌روزرسانی همان روز:** هر چهار فاز در موج چهارم پیاده شد — جزئیات و
> نتیجه‌ی آزمون‌ها در `CHANGELOG.md`. بندهای انجام‌شده پایین با ✅ علامت
> خورده‌اند؛ آنچه مانده (بندهای ۷ بخشی، ۱۰ جزئی و ۲۱–۲۳) برای جلسه‌های بعد.
این سند نتیجه‌ی یک ممیزی کامل کد است (پنج بررسی مستقل + راستی‌آزمایی خصمانه؛
همه‌ی موارد با مدرک `file:line` تأیید شده‌اند). هدف: بچه‌ها روی همین لیگ **هم
آموزش ببینند، هم مسابقه‌ی رسمی بدهند**.

خلاصه‌ی وضعیت: هسته‌ی شبیه‌ساز برای مسابقه **از نظر ساختاری سالم** است —
گام‌برداری متقارن دو ربات (هیچ‌کدام زودتر «نمی‌بیند»)، ساعت کنترل ۱۰ هرتزِ
زمانِ شبیه‌سازی، سنسورهای بدون نویز، و حرکت گربه/سگ کاملاً قطعی است. مشکل
مسابقاتی‌شدن در **اطرافِ** مسابقه است: تکرارپذیری، ثبت نتیجه، جدول، و ابزار داور.

---

## فاز ۱ — تکرارپذیری و عدالت (پایه‌ی هر مسابقه‌ی رسمی)

1. ✅ **گام زمانی ثابت فیزیک** — الان `dt` از فریم‌ریت مانیتور می‌آید
   (`index.html` حلقه‌ی اصلی؛ `engine.step(dt)` با dt خام). یعنی همان دو برنامه
   روی لپ‌تاپ ۶۰Hz و مانیتور ۱۴۴Hz نتیجه‌ی متفاوت می‌گیرند. راه‌حل: انباره‌ی
   گام ثابت (مثلاً همیشه گام‌های ۱/۶۰ ثانیه). **مهم‌ترین و ارزان‌ترین اصلاح.**
2. ✅ **بذر تصادف (match seed)** — تنها سه نقطه‌ی تصادفی در موتور هست: مقصد
   جابه‌جایی داور، جهت بعد از جابه‌جایی، و جای پشتیبان داک (`physics.js:304,
   905, 337`). یک PRNG بذردار (mulberry32) که داور بذرش را ست می‌کند و روی
   صفحه/گزارش ثبت می‌شود ⇐ با گام ثابت، «بذر + نقشه + دو فایل کد» = مسابقه‌ی
   ۱۰۰٪ قابل بازتولید، و **ری‌پلی رایگان** (ثبت فرمان چرخ‌ها در هر تیک کافی است).
3. ✅ **بودجه‌ی CPU هر تیم** — سقف ۵۰۰هزار دستور در هر تیک هست ولی سقف زمانِ
   واقعی نیست؛ یک برنامه‌ی سنگین می‌تواند فریم‌ریت سالن را برای هر دو تیم خراب
   کند. سقف چند میلی‌ثانیه‌ای در هر تیک + قانونِ جریمه برای تخلف مکرر.
4. ✅ **قانون خطای برنامه** — الان کرشِ کد فقط چرخ‌ها را صفر می‌کند و هیچ قانونی
   در RULES.md برایش نیست. تعریف: خطای مکرر = اخطار داور / باخت فنی.
5. ✅ **سیاست «نتیجه‌ی رسمی فقط از رایانه‌ی داور»** در RULES.md (تفاوت float بین
   مرورگرها).

## فاز ۲ — ثبت نتیجه، جدول و برگزاری (لایه‌ی تورنمنت محلی)

6. ✅ **ثبت خودکار نتیجه‌ی هر مسابقه** *(در همین به‌روزرسانی شروع شد — بخش
   «تاریخچه و امتیازات»)*: شناسه‌ی مسابقه، تیم‌ها، رده، نقشه، امتیازها،
   جریمه‌ها، وقت اضافه، تاریخ — در localStorage + خروجی CSV/JSON.
7. ✅ **دفتر ثبت تیم‌ها** — نام/مدرسه/اعضا یک‌بار ثبت شود، در مسابقه‌ها انتخاب
   شود (نه تایپ آزاد هر بار).
8. ✅ **قرعه‌کشی و جدول داخل خود برنامه** — ژنراتور گروه/حذفی برای ۸ تا ۳۲ تیم
   (فرمت قدیمی سایت: m1..m3، دو شانس مجدد، دو نیمه‌نهایی، فینال — ولی این بار
   محلی و بدون سرور)؛ صفحه‌ی براکت با دکمه‌ی Play که لابی را پر می‌کند
   (استخوان‌بندی `applyMatch` و `?match=` هنوز در کد هست).
9. ✅ **گره‌گشایی تساوی براکتی** — بعد از نردبان وقت اضافه (۳۵ ثانیه) نتیجه
   «تساوی» می‌ماند؛ برای بازی حذفی باید sudden-death یا شاخص دوم تعریف شود.
10. ✅ **آرشیو گزارش مسابقه** — دکمه‌ی دانلود PNG/JSON روی گزارش (heatmap +
    آمار) برای پرونده‌ی اعتراض.

## فاز ۳ — ابزار داور

11. ✅ **ثبت وقایع با زمان** — هر جابه‌جایی/کف‌خیس/توقف/تغییر سرعت/ویرایش خانه با
    ثانیه‌ی مسابقه در گزارش بیاید (الان فقط توست ۳ ثانیه‌ای است).
12. ✅ **جابه‌جایی بدون جریمه و جابه‌جایی با انتخاب نقطه** — الان دکمه‌ی داور
    همیشه جریمه می‌بُرد و مقصد تصادفی است؛ Shift+drag هم نه ثبت می‌شود نه در
    قوانین آمده.
13. ✅ **تأیید قبل از جابه‌جایی + امکان undo** — یک کلیک اشتباه الان ۵ کاشیِ
    برگشت‌ناپذیر هزینه دارد.
14. ✅ **False start** — شمارش معکوس قابل‌لغو نیست؛ دکمه‌ی «شروع مجدد» لازم است.
15. ✅ **بنر PAUSED روی صفحه** + ثبت مدت توقف.
16. ✅ **قفل حالت مسابقه‌ی رسمی** — ویرایش زنده‌ی خانه، سرعت ۲x و تعویض کد بعد از
    شروع، پشت PIN داور برود؛ ویرایش خانه الان بی‌صدا به‌عنوان نقشه‌ی دائمی
    ذخیره می‌شود.

## فاز ۴ — تحویل کد تیم‌ها (submission)

17. ✅ **فرمت تحویل رسمی**: فایل .py با سربرگ تیم/رده/تاریخ + SHA-256؛ نمایش هش
    روی صفحه هنگام لود؛ ثبت این‌که «واقعاً کدام کد دوید» در رکورد مسابقه.
18. ✅ **مهلت و قفل کد (code freeze)** — بعد از ددلاین، ایستگاه مسابقه فقط
    فایل‌های امضاشده را می‌پذیرد.
19. ✅ **اعتبارسنجی رده** — فایل FS در U19 بی‌اخطار لود می‌شود؛ لینت متغیرهای
    غیرمجاز هر رده.
20. ✅ **هلپر برای هر دو تیم** — الان خروجی هلپر فقط به ادیتور قرمز می‌رود و یک
    اسلات مشترک دارد (دو تیم روی یک دستگاه همدیگر را بازنویسی می‌کنند).

## فاز ۵ — آموزش (موازی با همه‌ی فازها)

*(بخش عمده در ۲۱ مرداد انجام شد — CHANGELOG را ببین: ردیابی زنده‌ی کد، گزارش
مسابقه با نکته‌ی مربی، چالش فنی پله‌ای، پل هلپر، برنامه‌های دوزبانه، و در همین
به‌روزرسانی: آموزش کامل هر رده، کد نمونه‌ی کامل «قهرمان» هر رده، و قوانین
داخل بازی.)*

باقی‌مانده: ۲۱. ویدئو/گیف کوتاه هر درس؛ ۲۲. کارنامه‌ی چاپی برای مربی (نتایج
چالش فنی + گزارش‌ها)؛ ۲۳. حالت تمرین دو نفره‌ی «آینه» (همان کد دو طرف).

---

### ترتیب پیشنهادی اجرا

| اولویت | چرا اول |
|---|---|
| فاز ۱ (بندهای ۱–۲) | بدون تکرارپذیری، هر نتیجه‌ی رسمی قابل اعتراض است |
| فاز ۲ (بندهای ۶–۸) | بدون ثبت نتیجه و جدول، «لیگ» فقط یک بازی است |
| فاز ۳ (بندهای ۱۱–۱۴) | دعواهای روز مسابقه را همین‌ها می‌خوابانند |
| بقیه | بعد از اولین رویداد آزمایشی، با بازخورد واقعی |
