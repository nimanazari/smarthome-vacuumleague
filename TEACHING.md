# آموزش صفر تا صد · Teaching the Smart Home League from zero

راهنمای مربی: با همین ترتیب درس بده. هر درس یک کدِ کامل و قابل‌اجرا دارد —
کپی‌اش کن داخل ادیتور تیم (یا «Load robot .py») و استارت بزن.
Coach's guide: teach in this order. Every lesson is a COMPLETE runnable
program — paste it into the team editor and press start.

**قانون طلایی · the golden rule:** برنامه‌ی ربات ۱۰ بار در ثانیه از بالا تا
پایین اجرا می‌شود. چیزی که در `wheelleft` / `wheelright` بگذاری تا اجرای بعدی
می‌ماند. `timer = timer + 1` یعنی هر ۱۰ تا، یک ثانیه.

---

## 🐣 فرست‌استپ (FS) — حس کن، واکنش نشان بده

### درس ۱ — راه برو
```python
wheelleft = 25        # چرخ چپ، تمام سرعت
wheelright = 25       # چرخ راست — مساوی یعنی مستقیم
```

### درس ۲ — به دیوار نخور (سنسور فاصله)
`front` می‌گوید جلوی ربات تا چند سانتی‌متر خالی است.
```python
if front < 40:        # چیزی نزدیک‌تر از ۴۰ سانتی‌متر است؟
    wheelleft = -18   # بچرخ (چرخ‌ها مخالف هم = چرخش درجا)
    wheelright = 18
else:
    wheelleft = 25    # راه باز است — برو
    wheelright = 25
```
همین ۶ خط یعنی «رباتی که راه می‌رود و به دیوار نمی‌خورد». بگذار بچه عدد
۴۰ را عوض کند و ببیند چه می‌شود.

### درس ۳ — بامپر، حس لمس
سنسور فاصله رو‌به‌جلو است؛ ضربه از بغل را `bumper` می‌فهمد (۱ = خوردیم).
```python
if bumper == 1:
    wheelleft = -20   # کمی عقب
    wheelright = -25  # یک چرخ تندتر = عقب‌گردِ کج، از دیوار جدا می‌شویم
elif front < 40:
    wheelleft = -18
    wheelright = 18
else:
    wheelleft = 25
    wheelright = 25
```

### درس ۴ — سنسور رنگ و فرش‌های نشانه
زیر ربات `color` است: `white` کف عادی، `green` فرش بزرگ (نصف سرعت، امتیاز
ندارد — ازش فرار کن!)، `purple / orange / cyan` فرش‌های کوچکِ **دمِ در** —
یعنی «اینجا یک در است».
```python
if color == green:
    wheelleft = -25   # روی فرش سبز نمان — امتیازی آنجا نیست
    wheelright = -20
elif front < 40:
    wheelleft = -18
    wheelright = 18
else:
    wheelleft = 25
    wheelright = 25
```

### با هلپر FS
صفحه‌ی «🤖 هلپر AI» همین قانون‌ها را با سؤال‌وجواب می‌سازد: سنسور را بکش،
جواب بده، ▶ بزن — مسابقه خودش شروع می‌شود. «🧩 هلپر بلاکی» همین را با
پازل می‌سازد و همان پایتون را می‌دهد.

---

## 🧭 پرایمری / U14 — بدان کجایی: قطب‌نما و شماره‌ی اتاق

U14 همه‌ی FS را دارد + دو حسِ جدید:
- `heading` = قطب‌نما (۰ راست، ۹۰ بالا، ۱۸۰ چپ، ۲۷۰ پایین)
- `room` = **شماره‌ی اتاقی که الان تویش هستی** (۰ هال، ۱ آشپزخانه،
  ۲/۳/۴ اتاق‌خواب‌ها، ۵ سرویس) و `clean1..clean5` = چند درصد آن اتاق
  رنگِ توست.

### درس ۵ — بچرخ به یک جهتِ مطلق
«۹۰ درجه بپیچ» حدس است؛ «رو کن به ۱۸۰» دقیق است:
```python
if heading > 185 or heading < 175:   # هنوز رو به ۱۸۰ نیستم
    wheelleft = -15                  # بچرخ تا قطب‌نما درست شود
    wheelright = 15
else:
    wheelleft = 25                   # حالا این خط را مستقیم برو
    wheelright = 25
```

### درس ۶ — شماره‌ی اتاق به چه درد می‌خورد؟ ⭐ مهم‌ترین درس U14
بدون `room`، ربات کور است: ممکن است ۳ دقیقه در یک اتاق بچرخد.
با `room` + `clean` ربات **مدیر** می‌شود: «این اتاق تمام شد؟ برو بعدی.»

**دستور خروج از اتاق** — سه قدم:
1. بفهم کجایی: `room == 2` یعنی داخل اتاق‌خواب ۱ هستم.
2. بفهم کارت تمام است: `clean2 > 80` یعنی ۸۰٪ اتاق ۲ مال من است.
3. برو سمت در: جهت درِ هر اتاق را از روی نقشه بخوان (مثلاً درِ اتاق ۲ رو
   به پایین است → ۲۷۰ درجه؛ فرشِ نشانه‌ی دم در هم با `color` تأییدش می‌کند).

```python
if room == 2 and clean2 > 80:        # اتاق‌خواب ۱ تمام شد
    if heading > 275 or heading < 265:
        wheelleft = -15              # رو کن به درِ اتاق (۲۷۰ = پایین)
        wheelright = 15
    else:
        wheelleft = 25               # مستقیم از در بیرون برو؛
        wheelright = 25              # به محض رد شدن room عوض می‌شود
elif bumper == 1:
    wheelleft = -20
    wheelright = -25
elif front < 40:
    wheelleft = -18
    wheelright = 18
else:
    wheelleft = 25
    wheelright = 25
```
نکته برای مربی: از بچه بپرس «از کجا می‌فهمی از اتاق بیرون آمدی؟» —
جواب: `room` دیگر ۲ نیست؛ آن لحظه قانونِ خروج خودش خاموش می‌شود.

**خروجِ چندمرحله‌ای:** بعضی اتاق‌ها با یک چرخش بیرون نمی‌آیی — اول رو به
درِ اتاق (مثلاً ۲۷۰)، رد که شدی رو به راهرو (مثلاً ۱۸۰). با یک متغیرِ
مرحله (`state`) پشت‌سرهمش کن:
```python
if room == 2 and clean2 > 80 and state == 0:
    state = 1                        # مرحله‌ی ۱: برو سمت در
if state == 1:
    if heading > 275 or heading < 265:
        wheelleft = -15              # رو کن به ۲۷۰ (درِ اتاق)
        wheelright = 15
    else:
        wheelleft = 25
        wheelright = 25
    if room != 2:                    # از در رد شدم!
        state = 2                    # مرحله‌ی ۲: حالا رو به راهرو
if state == 2:
    if heading > 185 or heading < 175:
        wheelleft = -15              # رو کن به ۱۸۰
        wheelright = 15
    else:
        wheelleft = 25               # در مسیرِ اصلی هستیم
        wheelright = 25
```
در هلپر همین کار با یک قانونِ ۳حرکته می‌شود: «Turn to ۲۷۰ ← Forward ←
Turn to ۱۸۰» — دکمه‌ی «🚪 قانون خروج» همین را می‌سازد.

### با هلپر U14
در هلپر، حس‌های «room» و «clean» را روی یک قانون بینداز: «اگر در اتاق ۲
بودم و اتاق ۲ بیش از ۸۰٪ تمیز بود → Turn to ۲۷۰ بعد Forward».
دکمه‌ی «🚪 قانونِ خروج از اتاق بساز» همین نمونه را یک‌ضرب می‌سازد تا
سر کلاس نشانش بدهی و بعد عددهایش را با بچه عوض کنی.

---

## 🔋 سکندری / U19 — موقعیت و انرژی

U19 همه‌ی بالا را دارد + `x, y` (موقعیت به سانتی‌متر)، `goto(x, y)` /
`atgoal`، باتری واقعی و شارژرِ ثابت (`dockx, docky` از ثانیه‌ی اول به هر
دو تیم داده می‌شود).

### درس ۷ — نقطه‌به‌نقطه
```python
if wp == 0:
    goto(500, 500)        # برو به (۵۰۰،۵۰۰) — خط مستقیم
    wp = 1
elif wp == 1:
    if atgoal == 1:       # رسیدم؟
        wp = 2
        goto(900, 300)    # بلافاصله مقصد بعدی
elif wp == 2:
    if atgoal == 1:
        wheelleft = 0     # مسیر تمام شد
        wheelright = 0
```
`goto` خط مستقیم می‌رود و دیوار را دور نمی‌زند — نقطه‌های میانی («ویا»)
باید بدهی. **هلپر مسیر** دقیقاً همین کار را می‌کند: روی عکسِ خانه نقطه
بزن، خودش ویاهای درها، گارد باتری، صفِ شارژر و همه‌ی کد را می‌نویسد.

### درس ۷.۵ — تابع‌ها ⭐ (سواد اصلی U19)
تابع یعنی «یک بلوکِ نام‌دار: یک بار تعریفش کن، هر جا خواستی صدایش بزن».
اسمش هم مال توست — عوضش کنی و جای صدازدن را هم عوض کنی، هیچ‌چیز نمی‌شکند.
```python
def go_kitchen():             # تعریف: یک بار
    goto(1100, 1100)

def twice(n):                 # با پارامتر و جوابِ برگشتی
    return n * 2

speed = twice(12)             # صدا زدن: هر جا لازم شد
if wp == 0:
    go_kitchen()
```
کدی که هلپر مسیر می‌سازد دقیقاً با همین ساختار است — دو تابع بالا تعریف
می‌شوند و پایینِ فایل صدا زده می‌شوند:
```python
def follow_route():           # مسیرِ من
    ...
def go_charge():              # سفرِ برگشت به شارژر
    ...
if charging == 1:
    go_charge()               # هر دهم‌ثانیه فقط یکی از این دو
else:
    follow_route()
```
تمرین کلاس: اسم `follow_route` را بگذار `gasht_zani` — هم در `def` هم در
صدازدن — و ببین هیچ فرقی نمی‌کند. بعد یک تابع سومِ خودت بنویس.

#### 📖 این دو تابع دقیقاً چه هستند و چطور کار می‌کنند؟

**قلب هر دو، متغیرِ `wp` است** («کدام ایستگاه؟»). هر نقطه‌ی مسیر و هر
نقطه‌ی راهِ شارژر یک شماره‌ی ایستگاه دارد. `wp` بین اجراها می‌ماند
(مثل timer) — پس ربات همیشه می‌داند کجای برنامه است.

**`follow_route()` — مسیرِ من.** یک if/elif بزرگ روی `wp`:
```python
if wp == 0:                 # ایستگاه ۰
    if atgoal == 1:         # به نقطه رسیده‌ام؟
        wp = 1              # ایستگاه بعدی
        goto(500, 500)      # و همان لحظه راهِ بعدی را شروع کن
    else:
        goto(300, 300)      # هنوز نه — به سمت همین نقطه بران
```
سه چیز را یاد بده:
1. `goto(x,y)` فقط «مقصد را تنظیم» می‌کند — خودِ بازی هر لحظه فرمان را
   می‌چرخاند تا برسد؛ رسید، `atgoal` می‌شود ۱.
2. چون `goto` دوباره صدا زده شود `atgoal` صفر می‌شود، اول `atgoal` را چک
   می‌کنیم بعد goto بعدی را می‌زنیم — و در **همان اجرا** goto بعدی را
   صدا می‌زنیم که ربات حتی یک دهم‌ثانیه هم بی‌هدف نماند.
3. بعضی ایستگاه‌ها «نقطه‌ی خودِ بچه» هستند و بعضی «ویا» — نقطه‌ی وسطِ
   درگاه که هلپر خودش اضافه کرده تا goto از دیوار رد نشود.

**`go_charge()` — سفرِ برگشت.** همان ساختار، ولی ایستگاه‌هایش
«زنجیره‌ی امن تا شارژر» است + آخرین ایستگاه، خودِ پد:
```python
elif wp == 9:                      # ایستگاه پد
    goto(dockx, docky)             # روی پد بایست (مختصات را بازی داده)
    if distto(dockx, docky) < 60 and atgoal == 0:
        gotoslow(dockx, docky)     # پد اشغال است؟ آرام در صف جلو بخز
    if battery > 90:               # پر شد؟
        charging = 0               # از حالت شارژ بیرون بیا
        wp = 4                     # به ایستگاهِ لنگر برگرد
        goto(760, 620)             # و راهش را همین حالا شروع کن
```

**پایینِ فایل — انتخابِ هر دهم‌ثانیه:**
```python
if battery < 35 and charging == 0:   # نگهبان باتری
    charging = 1                     # از این لحظه در «مأموریت شارژ»یم
    if wp == 0:                      # جدول پرش: از ایستگاهِ فعلی‌ام
        wp = 5                       #   بپر روی زنجیره‌ی امنِ همان ناحیه
        goto(940, 380)
    elif wp == 1:
        ...
if charging == 1:
    go_charge()                      # یا این
else:
    follow_route()                   # یا این — هرگز هر دو
```
`charging` یک پرچم است (۰/۱): تصمیم می‌گیرد کدام تابع اجرا شود. جدول پرش
هم برای این است که از هر جای مسیر، راهِ امنِ همان ناحیه به شارژر انتخاب
شود — نه خط مستقیمِ کور از وسط دیوارها.

سؤال‌های خوبِ کلاس: «اگر `charging = 0` را ننویسیم چه می‌شود؟» (ربات تا
ابد کنار پد می‌ماند)؛ «چرا wp بعد از شارژ به لنگر برمی‌گردد نه به صفر؟»
(لنگر نزدیک‌ترین ایستگاهِ دیدنی از پد است — از صفر شروع کند مسیر را از
اول می‌رود).

**تابع‌های آماده‌ی زمین** (این‌ها را خودِ بازی می‌دهد):
`goto(x,y)` برو به نقطه · `gotoslow(x,y)` همان با سرعت پیاده‌روی ·
`stopgoto()` رها کن · `distto(x,y)` فاصله تا نقطه (cm) ·
`angleto(x,y)` جهتِ نقطه (درجه) · `seconds(n)` تبدیل ثانیه به گام ·
`abs / min / max / round` ریاضی.

### درس ۸ — باتری
```python
if battery < 35 and charging == 0:
    charging = 1
    goto(dockx, docky)            # (در مسیر واقعی: از زنجیره‌ی امن هلپر)
if charging == 1 and battery > 90:
    charging = 0
    goto(500, 500)                # برگرد سر کار
```
قانون‌های زمین که باید سرِ کلاس بگویی: باتری ۰ = خزیدن با ۱۵٪ سرعت (می‌شود
تا شارژر خزید)؛ ۱۵ ثانیه یک‌جا ماندن = جابه‌جایی با جریمه؛ صف پشت شارژر
قانونی است.

---

## نقشه‌ی درس‌ها · lesson map (EN summary)

| # | league | lesson | new idea |
|---|---|---|---|
| 1 | FS | drive | wheels |
| 2 | FS | don't hit walls | `front` |
| 3 | FS | touch | `bumper` |
| 4 | FS | rugs | `color`, door markers |
| 5 | U14 | absolute turns | `heading` |
| 6 | U14 | **leave a finished room** | `room`, `clean1..5` |
| 7 | U19 | point to point | `x y`, `goto`, `atgoal`, `wp` |
| 8 | U19 | energy | `battery`, `dockx/docky`, the queue |

هر لیگ یک `program.py` نمونه‌ی کامل هم کنار خودش دارد
(`leagues/vacuum/<league>/program.py`) و کتاب کامل داوری در `RULES.md` است.
