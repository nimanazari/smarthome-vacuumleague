# آموزش صفر تا صد · Teaching the Smart Home League from zero

راهنمای مربی: با همین ترتیب درس بده. هر درس یک کدِ کامل و قابل‌اجرا دارد —
کپی‌اش کن داخل ادیتور تیم (یا «Load robot .py») و استارت بزن.
Coach's guide: teach in this order. Every lesson is a COMPLETE runnable
program — paste it into the team editor and press start.

**قانون طلایی · the golden rule:** برنامه‌ی ربات ۱۰ بار در ثانیه از بالا تا
پایین اجرا می‌شود. چیزی که در `wheelleft` / `wheelright` بگذاری تا اجرای بعدی
می‌ماند. `timer = timer + 1` یعنی هر ۱۰ تا، یک ثانیه.

---

## 🐣 فرست‌استپ (FS) — حس کن، واکنش نشان بده

### درس ۱ — راه برو
```python
wheelleft = 25        # چرخ چپ، تمام سرعت
wheelright = 25       # چرخ راست — مساوی یعنی مستقیم
```

### درس ۲ — به دیوار نخور (سنسور فاصله)
`front` می‌گوید جلوی ربات تا چند سانتی‌متر خالی است.
```python
if front < 40:        # چیزی نزدیک‌تر از ۴۰ سانتی‌متر است؟
    wheelleft = -18   # بچرخ (چرخ‌ها مخالف هم = چرخش درجا)
    wheelright = 18
else:
    wheelleft = 25    # راه باز است — برو
    wheelright = 25
```
همین ۶ خط یعنی «رباتی که راه می‌رود و به دیوار نمی‌خورد». بگذار بچه عدد
۴۰ را عوض کند و ببیند چه می‌شود.

### درس ۳ — بامپر، حس لمس
سنسور فاصله رو‌به‌جلو است؛ ضربه از بغل را `bumper` می‌فهمد (۱ = خوردیم).
```python
if bumper == 1:
    wheelleft = -20   # کمی عقب
    wheelright = -25  # یک چرخ تندتر = عقب‌گردِ کج، از دیوار جدا می‌شویم
elif front < 40:
    wheelleft = -18
    wheelright = 18
else:
    wheelleft = 25
    wheelright = 25
```

### درس ۴ — سنسور رنگ و فرش‌های نشانه
زیر ربات `color` است: `white` کف عادی، `green` فرش بزرگ (نصف سرعت، امتیاز
ندارد — ازش فرار کن!)، `purple / orange / cyan` فرش‌های کوچکِ **دمِ در** —
یعنی «اینجا یک در است».
```python
if color == green:
    wheelleft = -25   # روی فرش سبز نمان — امتیازی آنجا نیست
    wheelright = -20
elif front < 40:
    wheelleft = -18
    wheelright = 18
else:
    wheelleft = 25
    wheelright = 25
```

### با هلپر FS
صفحه‌ی «🤖 هلپر AI» همین قانون‌ها را با سؤال‌وجواب می‌سازد: سنسور را بکش،
جواب بده، ▶ بزن — مسابقه خودش شروع می‌شود. «🧩 هلپر بلاکی» همین را با
پازل می‌سازد و همان پایتون را می‌دهد.

---

## 🧭 پرایمری / U14 — بدان کجایی: قطب‌نما و شماره‌ی اتاق

U14 همه‌ی FS را دارد + دو حسِ جدید:
- `heading` = قطب‌نما (۰ راست، ۹۰ بالا، ۱۸۰ چپ، ۲۷۰ پایین)
- `room` = **شماره‌ی اتاقی که الان تویش هستی** (۰ هال، ۱ آشپزخانه،
  ۲/۳/۴ اتاق‌خواب‌ها، ۵ سرویس) و `clean1..clean5` = چند درصد آن اتاق
  رنگِ توست.

### درس ۵ — بچرخ به یک جهتِ مطلق
«۹۰ درجه بپیچ» حدس است؛ «رو کن به ۱۸۰» دقیق است:
```python
if heading > 185 or heading < 175:   # هنوز رو به ۱۸۰ نیستم
    wheelleft = -15                  # بچرخ تا قطب‌نما درست شود
    wheelright = 15
else:
    wheelleft = 25                   # حالا این خط را مستقیم برو
    wheelright = 25
```

### درس ۶ — شماره‌ی اتاق به چه درد می‌خورد؟ ⭐ مهم‌ترین درس U14
بدون `room`، ربات کور است: ممکن است ۳ دقیقه در یک اتاق بچرخد.
با `room` + `clean` ربات **مدیر** می‌شود: «این اتاق تمام شد؟ برو بعدی.»

**دستور خروج از اتاق** — سه قدم:
1. بفهم کجایی: `room == 2` یعنی داخل اتاق‌خواب ۱ هستم.
2. بفهم کارت تمام است: `clean2 > 80` یعنی ۸۰٪ اتاق ۲ مال من است.
3. برو سمت در: جهت درِ هر اتاق را از روی نقشه بخوان (مثلاً درِ اتاق ۲ رو
   به پایین است → ۲۷۰ درجه؛ فرشِ نشانه‌ی دم در هم با `color` تأییدش می‌کند).

```python
if room == 2 and clean2 > 80:        # اتاق‌خواب ۱ تمام شد
    if heading > 275 or heading < 265:
        wheelleft = -15              # رو کن به درِ اتاق (۲۷۰ = پایین)
        wheelright = 15
    else:
        wheelleft = 25               # مستقیم از در بیرون برو؛
        wheelright = 25              # به محض رد شدن room عوض می‌شود
elif bumper == 1:
    wheelleft = -20
    wheelright = -25
elif front < 40:
    wheelleft = -18
    wheelright = 18
else:
    wheelleft = 25
    wheelright = 25
```
نکته برای مربی: از بچه بپرس «از کجا می‌فهمی از اتاق بیرون آمدی؟» —
جواب: `room` دیگر ۲ نیست؛ آن لحظه قانونِ خروج خودش خاموش می‌شود.

### با هلپر U14
در هلپر، حس‌های «room» و «clean» را روی یک قانون بینداز: «اگر در اتاق ۲
بودم و اتاق ۲ بیش از ۸۰٪ تمیز بود → Turn to ۲۷۰ بعد Forward».
دکمه‌ی «🚪 قانونِ خروج از اتاق بساز» همین نمونه را یک‌ضرب می‌سازد تا
سر کلاس نشانش بدهی و بعد عددهایش را با بچه عوض کنی.

---

## 🔋 سکندری / U19 — موقعیت و انرژی

U19 همه‌ی بالا را دارد + `x, y` (موقعیت به سانتی‌متر)، `goto(x, y)` /
`atgoal`، باتری واقعی و شارژرِ ثابت (`dockx, docky` از ثانیه‌ی اول به هر
دو تیم داده می‌شود).

### درس ۷ — نقطه‌به‌نقطه
```python
if wp == 0:
    goto(500, 500)        # برو به (۵۰۰،۵۰۰) — خط مستقیم
    wp = 1
elif wp == 1:
    if atgoal == 1:       # رسیدم؟
        wp = 2
        goto(900, 300)    # بلافاصله مقصد بعدی
elif wp == 2:
    if atgoal == 1:
        wheelleft = 0     # مسیر تمام شد
        wheelright = 0
```
`goto` خط مستقیم می‌رود و دیوار را دور نمی‌زند — نقطه‌های میانی («ویا»)
باید بدهی. **هلپر مسیر** دقیقاً همین کار را می‌کند: روی عکسِ خانه نقطه
بزن، خودش ویاهای درها، گارد باتری، صفِ شارژر و همه‌ی کد را می‌نویسد.

### درس ۸ — باتری
```python
if battery < 35 and charging == 0:
    charging = 1
    goto(dockx, docky)            # (در مسیر واقعی: از زنجیره‌ی امن هلپر)
if charging == 1 and battery > 90:
    charging = 0
    goto(500, 500)                # برگرد سر کار
```
قانون‌های زمین که باید سرِ کلاس بگویی: باتری ۰ = خزیدن با ۱۵٪ سرعت (می‌شود
تا شارژر خزید)؛ ۱۵ ثانیه یک‌جا ماندن = جابه‌جایی با جریمه؛ صف پشت شارژر
قانونی است.

---

## نقشه‌ی درس‌ها · lesson map (EN summary)

| # | league | lesson | new idea |
|---|---|---|---|
| 1 | FS | drive | wheels |
| 2 | FS | don't hit walls | `front` |
| 3 | FS | touch | `bumper` |
| 4 | FS | rugs | `color`, door markers |
| 5 | U14 | absolute turns | `heading` |
| 6 | U14 | **leave a finished room** | `room`, `clean1..5` |
| 7 | U19 | point to point | `x y`, `goto`, `atgoal`, `wp` |
| 8 | U19 | energy | `battery`, `dockx/docky`, the queue |

هر لیگ یک `program.py` نمونه‌ی کامل هم کنار خودش دارد
(`leagues/vacuum/<league>/program.py`) و کتاب کامل داوری در `RULES.md` است.
